<%- include('./partials/layout-start', {title: title}) %> <%- include('./partials/menu', {isLogin: true}) %>

<div class="container mt-5">
    <div class="col-6 mx-auto">
        <h2 class="text-center">My profile</h2>
    </div>
    <div class="row">
        <div class="col-6 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form>
                        <div class="mb-3">
                            ID:
                            <input type="email" class="form-control" id="email" value="<%= user.id %>">
                        </div>
                        <div class="mb-3">
                            Email address:
                            <input type="text" class="form-control" id="email" value="<%= user.email %>">
                        </div>
                        <div class="mb-3">
                            Username:
                            <input type="text" class="form-control" id="email" value="<%= user.username %>">
                        </div>


                    </form>
                </div>
            </div>
        </div>
    </div>
</div>